# 更新說明 (Changelog)

本文件記錄 Taiwan Tender MCP 的變更細節。

## [0.0.4] - 2026-09-21

### 新功能 (Added)
- **`search_public_review`：依「公開閱覽期間」＋「機關名稱所在縣市」列出招標文件公開閱覽公告**。
  走官網獨立的「公開閱覽查詢」`readTpRead`（標案相關／公開閱覽查詢），
  這是**原本兩支查詢工具都查不到的公告池**：機關在正式招標前把招標文件掛出來給廠商表示意見，
  此階段**尚未招標、不能投標**，正式公告後才會出現在 `search_tenders`。
  參數：`reviewFrom`／`reviewTo`（民國或西元皆可）、`counties`（可省略縣／市，如「南投」）、
  `category`（採購性質 工程類／財物類／勞務類）、`maxPages`。
- **`countyInOrgName`（award-locations.ts）**：機關名稱推縣市的加寬版。
  原本的 `countyFromOrgName` 只比開頭，遇到「台灣電力股份有限公司台中區營業處」、
  「衛生福利部南投啟智教養院」一律回 null；新函式先比開頭、再往整串找。
  實測 115/09/01~115/09/21 全國 423 筆，判不出縣市的從 252 筆降到 192 筆。
  **舊函式行為未動**（決標 Excel、廠商檔案仍沿用），避免影響既有輸出。

### 重要差異 (Notes)
- **`category` 是官網端真的在篩**，不是本地過濾：實測 115/09/01~115/09/21
  工程 101＋財物 25＋勞務 297 ＝ 不限 423，三類互斥、不重不漏。
  注意這是粗分的「採購性質」，和 `search_tenders_by_category` 用的「標的分類代碼」是兩回事。
- **日期是「期間有交集就命中」**，不是只比起日：查 115/09/19~115/09/19 會撈到
  公開閱覽期間 115/09/17─115/09/30 的案子。這正好對應「某段時間可以閱覽的案子」。
- 踩到的坑（已寫進 `tpread-crawler.ts` 註解）：
  1. **日期必須送西元**（`2026/09/01`）。送民國回「共有 0 筆資料」，不報錯也沒提示，
     極易誤判成真的沒案子（`_smoke_tpread.mjs` 有這條回歸測試）。
  2. 表單標 `method="post"`，但 GET 帶 query string 一樣通，因此用 GET 以便帶分頁參數。
  3. 分頁可用（`d-<id>-p` 直接帶頁碼），`pageSize` 實測到 5000 都吃，
     但單頁 5000 筆的 HTML 有 3.5MB，故固定 500 一頁自動翻。
  4. 同一案的更正閱覽公告（公告次數 02 以上）會多出一列，已依機關＋案號併成最新一次，
     並在輸出標明合併了幾筆。

### 已知限制 (Known Limitations)
- 官網此查詢**沒有縣市／履約地點欄位**，`counties` 是比對機關名稱的近似作法：
  中央機關與國營事業（交通部鐵道局、台灣自來水、中華郵政…）名稱裡沒縣市的一律歸不出來
  （實測約占四成五），給了 `counties` 就會被排除，輸出會明寫被排除幾筆。
  該縣市機關在外縣市的案子也會被留下（「農業部林業及自然保育署臺中分署」的梨山案算臺中市）。
- 「查看」連結是**公開閱覽公告內頁**（`showTpReadDetail?pkTpRead=`），與標案內頁不同編號空間，
  **不可餵給 `get_tender_detail`**。

### 驗收 (Verification)
- `_smoke_tpread.mjs`：18 項（機關名稱推縣市 4 項、完整翻頁、欄位與期間解析、
  採購性質三類互斥與合計 4 項、期間交集語意、縣市篩選、民國年回 0 筆的回歸）。
  實測 115/09/01~115/09/21：官網 423 筆、抓取 423 筆、無截斷。
- `_smoke_mcp.mjs`：已把 `search_public_review` 加入必須註冊的工具清單。

## [0.0.3] - 2026-09-02

### 新功能 (Added)
- **`search_tender_archive`：可查已截止的歷史標案**。走官網「全文檢索（電子公報）」
  `readBulletion`，涵蓋民國 88 年起的公報，補上 `search_tenders`（只有等標期內）查不到的範圍。
  支援布林查詢語法、公報種類（招標／決標／公開閱覽及公開徵求／政府採購預告）、
  多年度（一次最多 3 年）與本地日期區間過濾；輸出自動分成「等標期內」與「已截止／歷史案」兩段。
  回傳的「查看」連結是 `tpam?pk=`，可直接餵給 `get_tender_detail`。
- **MCP server instructions**：把「查標案要分等標期內／已截止兩段回報、0 筆也要明寫」等
  行為規則寫進 server instructions，任何 client 掛上這支 MCP 就生效。

### 已知限制 (Known Limitations)
- 全文檢索**單次上限 100 筆／年度**：`pageSize` 帶 200/500/1000 一律只回 100 筆；
  官網分頁（`d-<id>-p`）是伺服器 session 狀態式的，GET／POST／完整 cookie jar／正確 Referer
  四種組合實測都只回沒有結果的表單頁（同一網址在瀏覽器可用）。
  因此改為「取最新 100 筆並誠實回報官網命中總數」，超出時提示縮小條件，不用瀏覽器自動化硬翻。

### 驗收 (Verification)
- `_smoke_archive.mjs`：36 項（年度解析、列表解析、截斷回報、歷史年度已截止判定、日期過濾、多年度合併）。
- `_smoke_mcp_archive.mjs`：stdio handshake 檢查工具註冊、instructions 內容與實際呼叫輸出分段；
  以「讀到回應才往下」取代定時 sleep，避免舊 `_smoke_mcp.mjs` 的假 FAIL。

## [0.0.2] - 2026-02-02

### 結構優化 (Refactoring)
- 重構專案架構，將爬蟲邏輯、業務邏輯與工具函數分離至 `services` 與 `utils` 目錄。
- 統一日期處理邏輯，建立 `src/utils/date.ts`。

### 爬蟲功能增強 (Crawler Improvements)
- **編碼修復**：引入 `iconv-lite` 自動處理政府網站的 Big5 編碼問題，修正搜尋結果亂碼。
- **解析優化**：改進標案案號與案名的拆分邏輯，提升抓取成功率。
- **Header 模擬**：優化瀏覽器 Header 模擬，降低被網站攔截的風險。

### 其他變更 (Other Changes)
- 修正 TypeScript 在 strict 模式下的型別錯誤。
- 清理根目錄，將測試腳本移至 `debug/` 資料夾。
- 更新版本號至 0.0.2 並同步 MCP Server 版本資訊。

## [0.0.1] - 2026-01-28

### 新功能 (Added)
- 初始版本：支援基本爬蟲搜尋與 MCP 工具介接。

---
*最後編輯: 2026-09-02*